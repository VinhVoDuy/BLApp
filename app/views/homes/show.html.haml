/ Background image
%img#background-image{:alt => "background image", :src => asset_url("1200x800.gif")}/
.boxed
  / Top header
  -# = render 'shared/top_header'
  / Header
  = render 'shared/header'
  / Revolution Slider
  = render 'shared/reservation_slider'
  / Reservation form
  -# - if current_user
  -#   = render 'shared/reservation_form'
  -# - else
  -#   = render 'shared/guest_reservation_form'
  = render 'shared/ups'
  = render 'shared/rooms', rooms: RoomType.all, title: 'Guests Favourite Rooms'
  = render 'shared/call_to_action'
  -# / Parallax Effect
  -# :javascript
  -#   $(document).ready(function(){$('#parallax-image').parallax("50%", -0.25);});
  -# %section.parallax-effect.mt100
  -#   #parallax-image
  -#     .color-overlay.fadeIn.appear{"data-start" => "600"}
  -#       .container
  -#         .content
  -#           %h3.text-center
  -#             %i.fa.fa.fa-star-o
  -#             STARHOTEL
  -#           %p.text-center An Exceptional Hotel Template!
  -#           %button.btn.btn-default.btn-lg.center-block Get template
  / Gallery
  = render 'shared/gallery'
  = render 'shared/comments_and_about'

  / Call To Action
  / Footer
  = render 'shared/footer'
  / Go-top Button
  #go-top
    %i.fa.fa-angle-up.fa-2x
