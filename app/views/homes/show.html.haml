/ Background image
%img#background-image{:alt => "background image", :src => asset_url("1200x800.gif")}/
.boxed
  = render 'shared/header'
  .container.overlay.grad#book-overlay
    .col-sm-9.visible-md.visible-lg
      .h5.white-color{style: "font-family: 'Kaushan Script'"} Welcome to the Blue River Hotel
    .col-sm-12.visible-xs.visible-sm
      .h5.white-color{style: "font-family: 'Kaushan Script'"} Welcome to the Blue River Hotel
    .col-sm-3.pull-right#book-now-btn.text-center.hidden-xs.hidden-sm.bookable
      %a{href: 'http://blue-river-hotel-3.hotelrunner.com/bv2/search'}
        .h4{style: 'padding-left: 50px;'}
          .fa.fa-calendar.white-color
          .white-color.reserve-btn RESERVATION
  .col-xs-12#book-now-btn-mobile.grad
    .col-xs-6.hidden-md.hidden-lg#menu
      %a{href: '#', id: 'menu'}
        .h4.pull-left
          .fa.fa-bars.white-color
          .white-color.reserve-btn MENU
    .col-xs-6.hidden-md.hidden-lg#book-now-btn.bookable
      %a{href: 'http://blue-river-hotel-3.hotelrunner.com/bv2/search'}
        .h4.pull-right
          .fa.fa-calendar.white-color
          .white-color.reserve-btn RESERVATION
  / Revolution Slider
  = render 'shared/reservation_slider'
  = render 'shared/amenities'
  = render 'shared/rooms', rooms: RoomType.all, title: 'Guests Favourite Rooms'
  / Gallery
  -# = render 'shared/gallery'
  -# = render 'shared/slidebox'
  = render 'shared/reviews_chart'
  = render 'shared/comments_and_about'

  / Call To Action
  / Footer
  = render 'shared/footer'
  / Go-top Button
  #go-top
    %i.fa.fa-angle-up.fa-2x

:javascript
  jQuery(document).ready(function(){
    var topOfOthDiv = jQuery("#book-overlay").offset().top;
    jQuery(window).scroll(function() {
      if (jQuery(this).scrollTop() > topOfOthDiv)
      {
        jQuery('#book-overlay').css({ position: 'fixed', top: '0', 'z-index': 9999 });
      }
      else
      {
        jQuery('#book-overlay').css({ position: 'static'});
      }
    });
    jQuery('.bookable').on('click', function(){
      HotelRunnerWidget.show(null);
    });

  })
